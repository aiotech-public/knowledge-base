---
id: ai-chat
title: AIO Chat — ассистент по трафику в интерфейсе
description: Встроенный AI-чат в шапке app.aio.tech — спросить AIO про свой трафик текстом, получить таблицы/графики/прогнозы, превью крео, алерты и справку «как устроен AIO». Тот же ассистент есть приложением AIO AI Copilot для iOS. Пока бесплатно (тест).
doc_type: how-to
builds: [erp, mtk]
related: [onboarding-tours, notification-center]
language: ru
updated: 2026-08-11
---

# AIO Chat — ассистент по трафику в интерфейсе

**`AIO Chat` — встроенный в интерфейс AI-ассистент: спрашиваешь про свой трафик обычным текстом, а он отвечает готовыми таблицами, графиками и прогнозами по данным твоего тенанта.** Открывается по иконке `AI` в шапке `app.aio.tech`. Клавиш и настроек не требует — задаёшь вопрос словами, ответ приходит прямо в окне чата.

Запускается кнопкой с радужным логотипом `AI` в правой части верхней панели. Иногда под иконкой всплывает короткая подсказка-тизер (например, «Спроси меня — я разбираюсь в трафике», «Построю прогноз по любой метрике») — по клику она тоже открывает чат; крестик на ней убирает подсказки насовсем.

Второй вход — кнопка **`Ask AI`** в попапе интерактивного тура по странице: она открывает тот же чат, уже подставив вопрос по текущему шагу тура ([how-to/onboarding-tours.md](onboarding-tours.md)).

Сейчас чат в режиме теста: под шапкой окна висит плашка **«Пока бесплатно — тестируем чат перед запуском AI-агентов»**. Пока идёт тест, обращения к ассистенту не тарифицируются.

---

## Что можно спросить

Ассистент отвечает на вопросы по данным твоего тенанта и по устройству AIO. Есть пять типов запросов; формулировки не нужно придумывать с нуля — на стартовом экране пустого чата висят готовые примеры, а под последним ответом чат подсказывает «следующие шаги» (разбор — раздел «Подсказки-чипы» этого дока).

### Аналитика по трафику

Вопросы по своим данным за период: «Покажи трафик за последние 30 дней», «Топ стран по выручке за неделю», «Разбей трафик по источникам за 7 дней», «Топ кампаний по ROI». Ответ — таблица, график или набор KPI-карточек с изменением к прошлому периоду.

### Прогноз метрики

Запросы вида «Построй прогноз визитов на неделю вперёд», «Спрогнозируй доход на следующий месяц». Ассистент рисует график: фактическая история сплошной линией и прогноз пунктиром, с затенённым коридором вероятного диапазона (`p10–p90`).

### Крео и история изменений

«Покажи превью активного объявления по кампании», «История изменений кампании за месяц». Отдаёт превью активного FB-объявления и отмечает на графиках вертикальными маркерами события — смену крео или правку кампании.

### Алерты и отчёты по расписанию

«Создай алерт: визиты меньше 100 за сегодня», «Покажи мои настройки уведомлений»; словами просят и так: «Присылай топ-10 кампаний по выручке каждый день в 9:00», «Уведомляй меня о конверсиях моих баеров». Чат заводит правила пяти типов из [mechanics/notification-center.md](../mechanics/notification-center.md): алерт по метрике (`metric_threshold`), отчёт по расписанию (`schedule` — табличный отчёт по cron, дефолт — ежедневно в 9:00 UTC) и событийные — создана конверсия (`conversion_created`, опционально фильтры по типам конверсий и минимальному ревеню), бан рекламного аккаунта FB (`fb_ad_account_banned`) и отвал его поллинга (`fb_ad_account_polling_failed`), у обоих опционально фильтр по рекламным кабинетам. Единственный тип, который через чат не создаётся, — правило по капе Destination (`destination_cap_threshold`): оно заводится руками на вкладке `Tenant alerts`.

Создание правила — это действие, меняющее настройки, поэтому ассистент сначала показывает карточку с кнопками `Разрешить` / `Отклонить` и выполняет его только после подтверждения (разбор — раздел «Подтверждение действий» этого дока).

Границы того, что делает чат, — чтобы не ждать от него лишнего:

- **Правило шлётся только тебе.** Аудитория правила, созданного через чат, всегда «себе» — на группу или весь тенант чат правило не заведёт.
- **Событийное правило создаётся включённым**, с антидребезгом 30 минут (`cooldown_minutes`) и автоматическим именем по типу события — имя можно задать своё сразу в запросе или переименовать потом.
- **Менять и удалять чат умеет, но только твои правила.** Из чата правится и удаляется правило любого типа (включить / выключить, переименовать, поправить конфиг) — при условии, что его создал ты сам и в этом же тенанте. Чужое правило чат не найдёт.
- **Флаг `Include my team members`** чат тоже ставит — тимлид начинает получать события своих баеров; механика флага — [mechanics/notification-center.md](../mechanics/notification-center.md).

### Как устроен AIO

Справка по продукту: «Как создать кампанию?», «Что такое AIO?», «Как запустить трафик?», «Как настроить Cost Update Strategy?», «Как устроены флоу и лендинги?».

## Чего чат не делает: границы ассистента

**Ассистент работает только внутри AIO — набор его действий закрытый.** Он не пишет и не исполняет код, не работает с файлами, не выполняет команды на сервере и не ищет в интернете: таких инструментов у него просто нет. Данные, которые он показывает, всегда ограничены твоим тенантом и твоим доступом — чужой трафик и чужие настройки через чат не достанешь.

## Подсказки-чипы: с чего начать и что спросить дальше

Чат подсказывает формулировки кликабельными чипами — на стартовом экране пустого чата и под последним ответом. Клик по чипу отправляет вопрос за тебя, печатать не нужно.

### Пустой чат: «Что ты умеешь?» и случайные примеры

На стартовом экране всегда есть чип «Что ты умеешь?», а рядом — подборка готовых примеров вопросов. Подборка случайная: примеры берутся из общего набора по пяти темам (аналитика, прогноз, крео, справка по AIO, алерты), но за раз показывается только часть набора — все заготовленные примеры сразу ты не увидишь. Каждое нажатие кнопки `Новый чат` пересобирает подборку заново, поэтому примеры меняются от чата к чату. Нужной формулировки в примерах нет — просто напиши вопрос словами.

### Под ответом: до трёх «следующих шагов»

Чипы «следующих шагов» появляются только под **последним** ответом ассистента — до трёх штук. Список не фиксированный: чипы досочиняет отдельный вызов по тексту твоего вопроса и полученного ответа, на языке вопроса. Прокрутив переписку вверх, под старыми ответами чипов не найдёшь — они остаются только у последнего.

Чипов может не быть вовсе. Подсказки не гарантированы: если их генерация не удалась, ответ приходит без чипов — на сам ответ это не влияет.

---

## Как выглядит ответ: таблицы, графики, воронки, карточки

Ответ приходит не только текстом — ассистент рисует готовые визуальные блоки поверх твоих данных, а не даёт сырые числа для копирования. Текстовые ответы форматируются как markdown; любой ответ можно скопировать иконкой копирования в углу сообщения.

### Таблицы и KPI-карточки

- **Таблица** — колонки с форматированием (деньги, проценты, флаги стран, даты), числовые метрики выделены.
- **KPI-карточки** — значение метрики + дельта к прошлому периоду со стрелкой тренда (зелёная вверх / красная вниз).

### Графики, воронки и превью крео

- **График** — линия, сплайн, площадь, столбцы или круговая; несколько серий, при разных масштабах — вторая ось справа; вертикальные маркеры-события на нужных датах.
- **Воронка** — этапы сверху вниз с шириной полосы по доле от первого шага и конверсией из предыдущего этапа.
- **Превью объявления** — активное FB-крео кампании (картинка, видео или встроенный предпросмотр).

---

## История чатов и сохранённые промпты

Диалоги сохраняются — к прошлому чату можно вернуться, а удачные запросы отложить в избранное.

- **Новый чат** — кнопка `Новый чат` в тулбаре окна начинает диалог с чистого листа; недавние чаты висят рядом кнопками с их названиями.
- **История** — кнопка `Ещё` (`История`) открывает полный список прошлых диалогов, отсортированных по последней активности.
- **Сохранённые промпты** — под удачным ответом жми `Лайкнуть промпт`, чтобы отложить свой запрос в список `Сохранённые` (звёздочка в тулбаре). Оттуда его можно повторно подставить в поле ввода или удалить.

Пока ассистент готовит ответ, в окне бежит статус («Думаю…», «Кручу таблицы…», «Считаю конверсии…») с таймером — это нормально для запросов, где нужно собрать и посчитать данные.

---

## Подтверждение действий (алерты и другие изменения)

Ассистент не меняет твои настройки молча — любое действие, которое что-то создаёт или правит (например, алерт на метрику), сначала выносится отдельной карточкой с кнопками `Разрешить` / `Отклонить`.

Пока действие не подтверждено, карточка висит в состоянии «Ожидает подтверждения». Жмёшь `Разрешить` — ассистент выполняет действие и показывает результат (`Разрешено`); `Отклонить` — отменяет (`Отклонено`). Карточка подтверждения переживает перезагрузку страницы: если закрыл окно с неотвеченным действием, при следующем открытии чата оно снова ждёт твоего решения.

---

## Есть ли ассистент на телефоне: приложение `AIO AI Copilot` для iOS

**Тот же ассистент доступен приложением для iOS — `AIO AI Copilot`.** Это не отдельный продукт: в приложении отвечает тот же ассистент, что и в окне чата в интерфейсе.

Ссылка на приложение — прямо в окне чата. В тулбаре окна `AIO Chat` есть кнопка **`iOS app`** (тултип `Open AIO AI Copilot in the App Store`), а на стартовом экране пустого чата — карточка с бейджем `App Store`. И кнопка, и карточка ведут на страницу приложения `AIO AI Copilot` в App Store и открываются в новой вкладке. Отдельного права или настройки у них нет: их видит каждый, у кого доступен сам чат.

---

## Где доступен

`AIO Chat` работает в билдах `ERP` и `MTK`, но иконка `AI` в шапке видна не всем: доступность ассистента регулирует команда AIO. Если в правой части верхней панели нет радужной иконки `AI` — значит для твоего тенанта чат сейчас не подключён, напиши команде AIO. Это тестовая фича — набор возможностей и доступность могут меняться.
